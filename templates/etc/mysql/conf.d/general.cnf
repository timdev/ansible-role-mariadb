[mysqld]
#
# Charset
#
character-set-server = utf8
collation-server     = utf8_unicode_ci
character-set-client = utf8

#
# MariaDB should really only listen on one address.
#
bind-address    = {{ mariadb_bind_addr }}

#
# Wait a long time: 24h
#
wait_timeout	= 86400